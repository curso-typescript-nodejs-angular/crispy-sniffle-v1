import * as repository from '../persistence/croptypeRepository';

describe('Teste integração CropType controller', () => {
    test('getCropTypes() deve retornar', () => {
        fail();
    });
});
