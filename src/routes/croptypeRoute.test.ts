import request from 'supertest';
import app from '../app';

describe('Teste unitário CropType route', () => {
    describe('GET /croptypes', () => {
        test('deve retornar status 200 OK e JSON', () => {
            fail();
        });
    });
});